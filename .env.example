# Portfolio Website Configuration
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Hashnode Configuration (Required)
# Your Hashnode publication domain (e.g., yourblog.hashnode.dev)
HASHNODE_PUBLICATION_HOST=your-blog.hashnode.dev
NEXT_PUBLIC_HASHNODE_PUBLICATION_HOST=your-blog.hashnode.dev

# GraphQL Endpoint (Optional - uses default if not set)
HASHNODE_GRAPHQL_ENDPOINT=https://gql.hashnode.com

# Optional: Hashnode Personal Access Token for enhanced features
# Get this from: https://hashnode.com/settings/developer
# Required for: Analytics, private posts, comments, and enhanced metadata
HASHNODE_PAT=your_hashnode_personal_access_token_here

# Webhook & Revalidation Settings (Optional)
# Secret key for webhook-based revalidation and security
# Generate using: openssl rand -hex 32
REVALIDATE_SECRET=your-webhook-secret-key
HASHNODE_WEBHOOK_SECRET=your_webhook_secret_here

# Development Settings (Optional)
# Enable debug mode for caching behavior during development
# NEXT_PRIVATE_DEBUG_CACHE=1

# Performance Settings (Optional)
# Cache revalidation time in seconds (default: 3600 = 1 hour)
# CACHE_REVALIDATE_TIME=3600

# Revalidation API Security (Configure in Vercel after deployment)
# Generate a secure random string for manual revalidation endpoint
# Use: openssl rand -hex 32
REVALIDATE_SECRET=your_revalidate_secret_here

# Development & Debugging (Optional)
# Uncomment for ISR cache debugging in development
# NEXT_PRIVATE_DEBUG_CACHE=1
